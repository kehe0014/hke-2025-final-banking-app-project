apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: banking-app-staging

resources:
  - ../../base

labels:
  environment: staging
  app.kubernetes.io/version: "1.0.0"

patches:
  - patch-deployment.yaml

images:
  - name: tdksoft341/tdk-banking-app
    newTag: "develop-ccf24421"
  - name: redis
    newTag: "develop-ccf24421"
